import java.util.*;

public class Rock {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        String n = s.nextLine();
        int size, i;
        for( i = 0; i < n.length(); i++) {
            if(str.charAt[0] == str.charAt[n.length()-1]){

            }
            else {

            }
        }
//i[0] == i[n-1]
//the number is not palindrome
// then i[1] == i [n-2]
    }
}
